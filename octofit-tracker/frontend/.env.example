# Required in Codespaces. Use the value from the CODESPACE_NAME environment variable.
VITE_CODESPACE_NAME=your-codespace-name

# When VITE_CODESPACE_NAME is empty, the app safely uses http://localhost:8000.
